local ReplicatedStorage = game:GetService("ReplicatedStorage")


local BuildUtil = {}
function BuildUtil.canPlaceRoomHere(roomDef, targetCF: CFrame, floor: number, existingRooms: {SerializedRoom}): boolean
	
end


return BuildUtil
